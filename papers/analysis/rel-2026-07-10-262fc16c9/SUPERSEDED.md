# SUPERSEDED analysis bundle

This Phase-6 analysis bundle belongs to release **`rel-2026-07-10-262fc16c9`**
(the pre-fix evidence produced before the C006 final-polish and M038
interaction-graph corrections, commit `af7efc534`).

It is **superseded by `papers/analysis/rel-2026-07-16-78f075cb0/`**, generated
2026-07-16 from the 51-run post-fix regeneration campaign.

It is retained (not deleted) because historical provenance chains reference its
checksums: the pre-2026-07-16 `_paper_tables` promotion manifest, the review-round
governance documents, and the supersession record itself. Do **not** cite it for
any current paper number — every generator resolves the live bundle via
`GSK_REL_ID` (default `rel-2026-07-16-78f075cb0`).

Retention rule: keep the two most recent release generations; older bundles may
be pruned once nothing in `papers/governance/` references them.
